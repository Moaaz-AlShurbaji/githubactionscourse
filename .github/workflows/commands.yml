on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: set API Key
        run: echo "API_KEY=12345" >> $GITHUB_ENV

      - name: print API key
        run: echo "This is the API KEY':' $API_KEY"
